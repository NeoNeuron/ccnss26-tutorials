numpy==1.26.4
scipy>=1.11
matplotlib>=3.8
pandas==2.2.2
scikit-learn>=1.2
networkx>=3.0
Cython
ipykernel
dandi
pynwb
nbformat
nbconvert
pytest
# Packages with stale constraints — install with --no-deps (see setup_env.sh):
#   allensdk declares numpy<1.24, pandas==1.5.3, scipy<1.11 — all incompatible
#     with Python 3.12 / Apple Silicon (no pre-built wheels for those old versions).
#     The allensdk code runs correctly with the modern versions above.
#   nlb-tools declares pandas<=1.3.4 — incompatible with pandas==2.2.2 above.
