"""Checkpoint cache for Colab-disconnect resilience."""


def save_checkpoint(*args, **kwargs):
    """Stub. Real implementation in a subsequent task."""
    raise NotImplementedError(
        "save_checkpoint is a stub; the real implementation lives in "
        "ccnss_helpers/_checkpoint.py and will be added in a follow-up commit."
    )


def load_or_compute(*args, **kwargs):
    """Stub. Real implementation in a subsequent task."""
    raise NotImplementedError(
        "load_or_compute is a stub; the real implementation lives in "
        "ccnss_helpers/_checkpoint.py and will be added in a follow-up commit."
    )
